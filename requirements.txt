# Lunar Lander RL - Python 3.12

# 설치 명령어 (순서대로 실행):
# 1. Remove-Item -Recurse -Force venv
# 2. C:\Users\shj\AppData\Local\Programs\Python\Python312\python.exe -m venv venv
# 3. .\venv\Scripts\Activate.ps1
# 4. python -m pip install --upgrade pip
# 5. pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cpu
# 6. pip install gymnasium h5py numpy matplotlib moviepy --only-binary :all:
# 7. pip install gymnasium[box2d]

torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
gymnasium>=0.27.0
h5py>=3.7.0
numpy>=1.21.0
matplotlib>=3.5.0
moviepy>=2.0.0
